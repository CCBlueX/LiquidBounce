name: 'Close stale issues and PRs'
on:
  schedule:
    - cron: '30 1 * * *'

permissions:
  actions: write
  issues: write
  pull-requests: write

jobs:
  stale:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/stale@v10
        with:
          stale-issue-message: 'This issue is stale because it has been open 60 days with no activity. Remove stale label or comment or this will be closed in 365 days.'
          stale-pr-message: 'This PR is stale because it has been open - days with no activity. Remove stale label or comment or this will be closed in 365 days.'
          close-issue-message: 'This issue was closed because it has been stalled for 365 days with no activity.'
          days-before-stale: 60
          days-before-close: 365
          days-before-pr-close: -1
          exempt-all-pr-assignees: true
